- 模块功能
 1. 编译期检查模型定义是否合理
    代码编译阶段检查：前台业务订单模型与统一订单模型是否有冲突，存在冲突编译期报错，将问题阻拦在编译阶段进行解决

 2. 注解定义
    Aggregate ：聚合根

    Entity ：实体

    ExcludeField ：注解标识后，被标注属性数据不会传送到中台

    ValueObject ： 值对象


测试:
javac -cp /path/to/aptdemo-1.0-SNAPSHOT.jar SomeTestClass.java

